# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'therappeut' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Google Firebase Pods for therappeut
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  
  # Additional pods
  pod 'SVProgressHUD'

  target 'therappeutTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'therappeutUITests' do
    # Pods for testing
  end

end
